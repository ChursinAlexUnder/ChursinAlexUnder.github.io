#include <iostream>
#include <fstream>
#include <string>
#include <windows.h>
using namespace std;
int main() // Сервер
{
	setlocale(LC_ALL, "Russian");
	cout << "server is working!" << endl;
	while (true) // как сделать, чтобы выходил при esc?
	{
		string str;
		ifstream fin("E:\QUESTION1.txt");
		fin.seekg(0, ios::end);
		int nsize = fin.tellg(), osize = fin.tellg();
		while (nsize == osize)
		{
			/*osize = nsize;*/ // Зачем это?
			Sleep(500);
			fin.seekg(0, ios::end);
			nsize = fin.tellg();
		}
		fin.seekg(osize, ios::beg);
		getline(fin, str);
		cout << str;
		cout << endl;
		fin.close();
	}
	return 0;
}
