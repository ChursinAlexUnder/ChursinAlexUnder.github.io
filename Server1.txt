#include <iostream>
#include <fstream>
#include <string>
#include <windows.h>
using namespace std;
int main() // Сервер
{
	int nsize = 0, osize = 0;
	string str;
	char s;
	cout << "server is working!" << endl;
	ifstream fin("Z:\QUESTION1.txt");
	nsize = fin.tellg();
	while (nsize == osize)
	{
		osize = nsize;
		Sleep(1111);
		fin.seekg(0, ios::end);
		nsize = fin.tellg();
	}
	fin.seekg(osize, ios::beg);
	getline(fin, str);
	cout << str << endl;
	fin.close();
	return 0;
}
